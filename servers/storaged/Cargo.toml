[package]
name = "redoubt-storaged"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[[bin]]
name = "redoubt-storaged"
path = "src/main.rs"
test = false

[dependencies]
redoubt-userlib = { path = "../../userlib" }
redoubt-crypto = { path = "../../crypto" }

[build-dependencies]
redoubt-updated = { path = "../updated", artifact = "bin", target = "x86_64-unknown-none" }
